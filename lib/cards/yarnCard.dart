import 'package:flutter/material.dart';

class Yarn extends StatelessWidget {
  const Yarn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Yarn'));
  }
}