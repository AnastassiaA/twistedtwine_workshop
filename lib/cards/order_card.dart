import 'package:flutter/material.dart';

class Orders extends StatelessWidget {
  const Orders({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('The order'));

    /* 3 TABS,  first tab has order info, second tab has a details tab jus like
    * your current project timer app and the third tab has all the money details*/
  }
}
